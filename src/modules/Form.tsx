import React from "react";
import { BrowserRouter as Router, Routes, Route, Link } from "react-router-dom";

const Form = () => {
  return (
    <div>
      <h1>Form</h1>
    </div>
  );
};

export default Form;
